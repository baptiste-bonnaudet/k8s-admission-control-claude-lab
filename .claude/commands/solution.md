Read the reference solution from the current exercise's `solution/` directory. If the user has not specified an exercise, ask which one they want.

Show the complete solution YAML without omitting anything.

Then explain each design decision — not just what the fields do, but why each choice was made:
- Why this field path and not another
- Why this operator or expression form
- Why this scope (namespace selector, resource rules)
- What would break or be less correct with a naive alternative

After the explanation, invite the user to compare with their own attempt: "How does this compare to what you had? What surprised you?"
